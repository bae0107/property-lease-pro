spring:
  security:
    user:
      name: user
      password: user
  # H2 内存数据库配置
  datasource:
    url: jdbc:h2:file:D:/h2data/PROPERTY_LEASE_MAIN;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE;TIME ZONE=Asia/Shanghai
    driver-class-name: org.h2.Driver
    username: sa
    password: sa

  #  datasource:
  #    url: jdbc:mysql://rm-uf657oh47p060763poo.mysql.rds.aliyuncs.com/PROPERTY_LEASE_DEVICE?characterEncoding=utf8&serverTimezone=Asia/Shanghai
  #    driver-class-name: com.mysql.cj.jdbc.Driver
  #    username: shibowen
  #    password: Shi64395338

  # H2 控制台配置（方便调试）
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false
  sql:
    init:
      platform: h2
      mode: always

  #  # Liquibase 配置
  #  liquibase:
  #    default-schema: PUBLIC
  #    liquibase-schema: PUBLIC

  # 1. 禁用 Redis 自动配置
  # 启动时不要去检查 Redis 连接。
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

    # 2. 启用内存缓存
    # 使用 ConcurrentHashMap 作为底层实现。
  cache:
    type: simple
  jooq:
    sql-dialect: h2

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org:
      jooq: DEBUG
security:
  mode: mock
  service-name: ${spring.application.name}
  mock-user:
    user-id: 1
    permissions:        # 留空则 @PreAuthorize 会返回 403（用于测试无权限场景）
      - "iam:user:write"
      - "iam:user:create"
      - "iam:user:read"
      - "iam:user:password:reset"
      - "iam:role:read"
      - "iam:role:write"
  jwt:
    user:
      secret: local-user-jwt-secret-for-development-only
      expiration: 1800
    service:
      secret: local-service-jwt-secret-for-development-only
      expiration: 300

server:
  forward-headers-strategy: framework